{
  "name": "ugcnp-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "UGCNP end-to-end tests (Playwright) — UI flows against the running stack",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}